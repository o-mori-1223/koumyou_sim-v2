numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
streamlit==1.58.0
plotly==6.8.0
colour-science==0.4.7

# rcwa_mh(C++/pybind11)は RCWA2D/ 内にビルド済みバイナリ(.pyd / .so)を同梱しているため
# pipインストールは不要。ソースからの再ビルドが必要な場合のみ pybind11 を追加でインストール
# すること(pybind11==3.0.4 で動作確認済み)。
# S4バックエンドはWSL上の別環境(~/s4env)でのみ動作する。HANDOVER.md 2章参照。
