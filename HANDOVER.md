# 引継ぎメモ — 構造色シミュレータ / RCWA・S4・COMSOL比較検証

最終更新: 2026-09-24

## 1. プロジェクトの目的

列（column）ベースの周期構造（基板段差＋TiO2/SiO2多層膜）の反射率をRCWAで計算する
Streamlitアプリ（`R8_koumyou_v3.py`、旧名R9_koumyou_v2.py）がある。自作RCWAソルバー
`rcwa_mh`（C++/pybind11）の計算結果が、外部ツール（COMSOL FEM, Gsolver）の結果と
一致しないという指摘があり、原因調査を継続中。加えて、独立検証用に**S4**
（Stanford Stratified Structure Solver）バックエンドを新規構築した。

**【2026-08-24 大きな進展】** 列＋段差DBR構造でrcwa_mh/S4とGsolverが食い違っていた
原因がついに特定できた: **GsolverのORDERS（Fourier打ち切り次数）をデフォルトの
低い値のまま使っていたこと、および構造をGraphical EditorでCanvas手描きしたこと
（寸法がCanvas Gridに量子化される）の両方が必要な誤差要因だった**。ORDERS=43以上
＋テキストインポート（正確な寸法）の組み合わせで、rcwa_mh・S4・Gsolverの3ツールが
ほぼ完全に一致することを確認済み（詳細は4節参照）。COMSOLとの不一致（st235・
旧構造）についても、メッシュ密度が同種の役割を果たしている可能性が高く、次に
確認すべき筋の良い仮説として残っている。

**【2026-08-26】** 上記の結論を、koumyouアプリ自体（`R8_koumyou_v3.py`、nOrder=43
＋正しい周期設定）でも全角度（-80〜80°、697点）にわたって再確認し、
**rcwa_mh・S4・Gsolver・koumyouアプリの4者が一致することを確認**（詳細は4節）。
併せてkoumyouアプリに0°/30°/60°の色スウォッチ表示機能を追加。一方、新しい膜厚
レシピ（TiO2=51nm/SiO2=81nm、周期1200nm）ではGsolverとkoumyouの色が違って見える
という新しい指摘があり、koumyou側の計算・色変換ロジック自体には問題がないことは
確認できたが、Gsolver側の実測数値との最終突き合わせがまだ済んでおらず**未解決の
まま次回セッションに持ち越し**（詳細は4節の該当項目、次にやるべきことも記載）。

**【2026-08-27】** 2026-08-26に未解決だった2col_51_81構造（TiO2=51nm/SiO2=81nm、
周期1200nm）について、ユーザーがGsolverとkoumyouの反射率実測値を直接突き合わせ、
**「完全に一致した」と報告**。ただし、このセッションでは提供されたCSV
（`angle_Rs_タイムスタンプ.csv`形式）がGsolverの生データなのか、koumyouアプリ自身の
「Angle CSV Rs」ダウンロード機能（[R8_koumyou_v3.py:944](R8_koumyou_v3.py#L944)付近）
の出力を再エクスポートしたものなのかの確認が完了する前に、別の話題（下記の
なだらか基板上げプロファイル機能）へ移ったため、**このセッションでの独立した
数値検証（mean|diff|等の算出）は行っていない**。ユーザーの一致報告は信頼できると
見てよいが、次回セッションで機会があればCSVの出所だけ確認しておくとよい。

**【2026-09-07】色スウォッチ・角度チャート・スペクトルチャートを
`Gsolver'S COLOR.html`（参考のスタンドアロンHTMLツール）と完全に見た目・機能を
統一**: `R8_koumyou_v4.py`の`st.color_picker`/Plotlyベースの表示を、そのHTML
ツールのCSS/JSをほぼ検証済みのまま移植した`RCWA2D/color_display.py`+
`RCWA2D/color_display_template.html`（`st.components.v1.html()`で埋め込み）に
置き換えた。統一に伴い、Rp/Rs切替をスウォッチ・角度チャート・スペクトルチャートで
別々にあった3つのラジオボタンから1つ（`disp_pol`）に統合し、角度チャートの表示
波長もHTML版と同じ固定5本(400/470/540/600/700nm)にした（`sel_wls`マルチセレクトは
削除）。「スクリーンショット用に表示」ボタンで3セクション全てが連動してcapture-mode
表示に切り替わる（ヘッダー非表示含む）。詳細はコード内のDEVIATIONコメント3箇所
（スウォッチ用matrixを掃引matrixから独立させた・密な角度掃引での凡例デフォルト
選択をnearestValue化・`spectrumHideOffLegend`初期値をtrueに変更）を参照。
実際に`streamlit run R8_koumyou_v4.py`で起動しての最終的な目視確認（HTML版との
並べ比較）はまだ行っていない。

**【2026-08-27】なめらか基板上げプロファイル（Graded raise）機能を追加**:
ユーザーから、実際のリソグラフィ/エッチング断面に近い「肩の丸まったなだらかな
基板上げプロファイル」をkoumyouアプリで扱えるようにしたいとの要望があり、
次の式が提示された:
```
h(s) = H * exp(-|((2*s-x1-x2)/(x2-x1))|^n),  n = 0.02*(x2-x1)
```
（H=基板上げのピーク高さ、x1/x2=列の絶対左右端、s=横方向位置。列の幅が
広いほど次数nが大きくなり、肩の切り立った台形に近づく。列端(s=x1,x2)でも
高さはH/e≈0.37Hまでしか下がらず、真の0への収束は隣接列側でなだらかに起こる）。

方針検討でPlan agentによるレビューを実施し、当初案の2つの実バグ
（周期全体を均等分割すると狭い列がfilm_layersを失いうる／周期境界での
裾の回り込みを見落としていた）を修正した上で実装:
- **列ごとに自分自身の幅をn_slices等分**する方式を採用（周期全体の均等分割
  ではない）。所属列は常に一意に決まりfilm_layersが消失することはなく、
  幅の合計も厳密に保存される。
- 裾が隣接列へ物理的に侵入することを許容（列内でクリップしない）。かつ
  周期境界(s=0/pitch)をまたいだ裾の回り込みも s±pitch の2次像で評価。
- `abs(u)**n`の計算はnumpy経由にし、極端な列幅でも`OverflowError`にならず
  `inf→exp(-inf)=0.0`に自然減衰するようにした。
- 既存の検証済みコア（`columns_to_layers`/`_expand_to_bands`）には一切
  手を入れず、「列リスト→より細かい列リスト」への前処理関数
  `expand_graded_columns()`（`RCWA2D/structure_builder.py`）を新設し、
  その出力をそのまま渡す設計にした。'graded'な列が1つも無ければこの新関数
  自体を呼ばないため、既存構造の計算結果は完全に不変（回帰リスクなし）。
- アプリ本体は`R8_koumyou_v3.py`を直接編集せず、コピーした
  **`R8_koumyou_v4.py`** に実装（過去のv2→v3と同じ、バージョンファイルを
  分ける慣習に合わせた）。v3は無変更のまま。列UIに「上げ形状」選択
  （sharp/graded）、graded列がある場合のみ表示される分割数(n_slices)入力
  （'taper'のn_slicesと同じ2〜100レンジ、デフォルト10）を追加。
- **注意（性能）**: 上げ高さが列内で連続的に変わることで、その上の膜層境界も
  それぞれズレるため、`columns_to_layers`が生成するRCWA解析層数は概ね
  n_slices×膜層数のオーダーで増える。特にS4は1点あたりの計算が遅いため、
  graded機能使用時はS4計算が大きく遅くなりうる（UIのhelpテキストに明記済み）。
- **検証結果**: 幅保存則・列端でH/eになること・sharp隣接列への裾の侵入と
  周期境界での回り込み・極端な幅でのオーバーフロー未発生・全列sharpなら
  新パスが呼ばれず既存動作が不変であること、をWindowsネイティブの数値
  チェックで確認済み。実際にrcwa_mhで2列構造（幅900 sharp + 幅300 graded,
  H=235nm, TiO2/SiO2ペア3組）を計算しn_slices収束も確認: n_slices=10/20/40を
  基準n_slices=80と比較してmean|diff|=0.019/0.012/0.002と単調収束（詳細な
  比較スクリプトはこのセッションでは一時実行のみで未保存）。Streamlit
  AppTestでも列追加→subst_raise設定→raise_modeをgradedに切替→
  graded_n_slices操作、の一連の操作が例外なく動作することを確認済み。
- **未実施（次回セッションへ）**: 実際に`streamlit run R8_koumyou_v4.py`
  （WSL）を起動しての目視確認（断面図がユーザー提供の参考画像の丸みを
  帯びた形状と一致するか）はこのセッションでは行っていない。またS4
  バックエンドでのgraded構造の動作確認もWindows側では未実施（S4はWSL限定）。

**【2026-08-27 マシン移行】** このPCのスペック不足のため、上記の目視確認以降の
作業は**別のPCで継続する**方針になった。移行先PCでのセットアップ手順:
1. GitHubリモート`origin`（`https://github.com/o-mori-1223/koumyou_sim-v2.git`、
   このセッションで確認済み）から`git clone`（または既にクローン済みならこの
   セッションで作成したコミットを`git pull`）する。**このセッション終了時点で
   `HANDOVER.md`, `RCWA2D/structure_builder.py`, `R8_koumyou_v4.py`,
   `requirements.txt`にまだコミットされていない変更が残っている**ので、
   移行前にこのPC側でコミット（可能なら`git push`）しておくこと。
2. Windows側: Python **3.11系**の仮想環境を作成し
   （`RCWA2D/rcwa_mh.cp311-win_amd64.pyd`がcp311向けビルドのため、
   3.12以降のPythonでは`import`できず再ビルドが必要になる）、
   新規追加した`requirements.txt`から`pip install -r requirements.txt`する
   （streamlit/numpy/pandas/scipy/plotly/colour-scienceの版数を固定済み）。
   `rcwa_mh`自体はビルド済み`.pyd`が`RCWA2D/`直下にあるので追加ビルド不要。
3. WSL側（S4を使う場合）: 2.1〜2.2節の`~/s4env`環境を移行先PCでも改めて
   構築する必要がある（このセッションでは未実施・未検証。WSL2 + Ubuntuの
   セットアップからやり直しになる可能性が高い）。
4. 移行後、まず`streamlit run R8_koumyou_v4.py`（Windows側`.venv`で可、
   S4は使わずrcwa_mhのみのgraded raise機能確認だけならWSL不要）を起動し、
   本メモの「未実施」項目（断面図の目視確認）から再開すること。

**【2026-09-17 新規テーマ・進行中】孤立ナノ円盤の3D FDTD散乱シミュレータ（MEEP）**:
ここまでのRCWA/S4/Gsolver/COMSOL比較検証とは別の、**全く新しいテーマ**。ユーザーが
「ナノ円盤構造」の解析を希望。周期配列ならS4の2D格子モード（未着手・別タスク）で
対応できるが、ユーザーが選んだのは**基板上の孤立円盤1個**（周期性なし）で、これは
RCWA/S4では原理的に扱えない。自作Python/numpyで3D FDTDをゼロから書く案は
「正しさ・速度とも数週間規模の検証が必要で非現実的」と判断し、ユーザー了承のもと
**MEEP**（MIT製オープンソースFDTD、C++コア+Pythonバインディング）をWSLに導入し
ラッパーを書く方針に決定。設計はPlan agentのレビューを2回受けている
（MEEP公式のsphere-in-vacuum Mie散乱チュートリアルを実際に取得して技術詳細を検証）。

*スコープ（Phase 1、承認済み）*: 法線入射のみ・TiO2円盤（無損失, index=2.30）・
SiO2基板（無損失, index=1.45。損失ありのSUSはPhase 1.5として後日）・
散乱効率スペクトル1本・0°色スウォッチ1個。角度掃引(30°/60°)はPhase 2として
明確に先送り——孤立散乱体（全面PML、周期性なし）では1回の広帯域パルスで
単一角度を波長全体にわたって維持できず、角度×波長ごとに個別runが必要になるため、
法線入射に対し概算100〜250倍のコストになると試算済み（詳細はPlanエージェントの
検討過程、および下記ファイルのdocstring参照）。

*環境構築（完了）*: WSLにcondaが無かったため**Miniforge3を新規インストール**
（`~/miniforge3`）。**教訓: `conda create -n mp -c conda-forge pymeep streamlit numpy`
を1回のコマンドでやると、streamlitのconda版が引き込む`libarrow`(pyarrow用の
巨大なC++ライブラリ)のダウンロードでネットワークエラーが起き2回失敗した
（`| tail`でパイプすると失敗時もexit code 0に化けて見えるので要注意）。
`conda create -n mp -c conda-forge pymeep numpy`で軽量に作ってから、
`conda run -n mp pip install streamlit`で後からpip経由で入れると確実に成功した**。
`mp`環境: pymeep 1.34.0 + numpy(conda) + streamlit 1.64.0(pip)。起動は
`run_meep3d.sh`（LF改行で保存済み、`conda activate mp`後`streamlit run meep3d_app.py`）。

*新規ファイル*: `MEEP3D/`パッケージ（`materials.py`: data/nk読み込み+`mp.Medium`化、
`geometry.py`: セル/PML/flux box(6面, 底面はz=0より`box_margin`だけ上)/対称性の
構築（3モード`vacuum`/`bare_substrate`/`disk`で完全に同一形状、geometryリストだけ
差し替え）、`simulate.py`: 3-run減算法（Run A=真空でI0取得、Run B=基板のみで
ベースライン記録、Run C=基板+円盤でRun Bを`load_minus_flux_data`で差し引き）で
`Q_scatter,up`スペクトルを返す、`color_glue.py`: 既存の`RCWA2D/color_display.py`
（前セッションで凍結済み、無改造）にそのまま接続）。`meep3d_app.py`（新規
Streamlitページ、R8_koumyou_v4.pyとは別・無改造）。検証用`diag_meep_sphere_mie.py`
（MEEP公式Mieチュートリアルの再現、`MEEP3D.simulate._scattering_efficiency_from_fluxes`
と同じ算術コードを検証）。

*検証状況（途中、次回に持ち越し）*:
- **sphere-in-vacuum（基板なし）は動作確認済み**。実装当初は2つの実バグがあった:
  (1) 真空run/球ありrunで対称性設定が食い違っており(`symmetries=[] if with_sphere
  else symmetries`という誤ったコード)、`load_minus_flux_data`でのデータ形状不一致
  によりクラッシュ。両runで同じ対称性を使うよう修正。
  (2) 周波数範囲(frq_cen=1.0, dfrq=2.0)が0を含んでおり`1/freq`でゼロ除算警告。
  WebFetchでチュートリアルページから正確な数値（`wvl_min=2πr/10`, `wvl_max=2πr/2`,
  `dpml=dair=0.5*wvl_max`, `resolution=25`）を再取得し、それに合わせて修正。
  修正後、複数のMie共鳴ピークを持つ物理的に妥当なQsca値（オーダー1.6〜3.6、
  最初の帯域端1点だけソース強度不足による外れ値——これは想定内）が得られた。
- **基板ありの円盤ケースは未解決**。パイプライン自体はクラッシュせず、指定した
  波長範囲(400-800nm)も正しく反映されるようになった（`fcen`/`df`をソース帯域用
  ・flux監視用で分離する追加修正が必要だった）が、**`Q_scatter,up`が一貫して
  小さい負の値（-0.0007〜-0.0022程度、resolution=30・n_freq=11のスモークテスト）
  になっており、物理的に正しくない**（散乱効率は非負のはず）。滑らかに波長変化
  しており乱数的なノイズには見えないため、何らかの系統的な原因（サイン規約の
  誤り、またはbox_margin=0.1umが波長(400-800nm)に対して近すぎ、円盤近傍の
  近接場/エバネッセント成分を誤って拾っている可能性）が疑われるが、
  **未診断のままこのセッションを終えた**。
- **次回セッションでまず試すこと**（優先順）:
  1. ゼロテスト: 円盤材質=基板材質（コントラストなし）にして`run_scattering_spectrum`
     を実行し、結果がほぼ0に潰れるか確認する。潰れなければ、原因は特定の材質では
     なくbox/幾何設定そのものにあると切り分けられる。
  2. `box_margin`を0.1um→0.3〜0.5um程度に広げ、近接場汚染仮説を検証する。
  3. 符号が正しくなってから、解像度収束確認（30→60→100 px/um）を行う。
  4. これらに合格して初めて、Phase 1a（TiO2円盤/SiO2基板、400-800nm）の
     数値を信用してよい。
- HANDOVER更新時点で`meep3d_app.py`（Streamlit UI）自体はまだ一度も
  ブラウザで起動確認していない（`run_meep3d.sh`経由での動作確認も未実施）。

**【2026-09-24】v3/v4アプリの単一列DBR構造で「0次光以外=0」＋S4一致を再確認（コード変更なし）**:
別セッションで、koumyouアプリのColumn builderで**列を1本だけ**使った構造（幅=ピッチ
全体300nm、SiO2 65.5nm/TiO2 41.3nmペア×7＋TiO2単層41.3nm、基板n=1.5定数——
[R8_koumyou_v4.py](R8_koumyou_v4.py)のスクリーンショットで実際に問われた構成）について
2つの質問があった: 「0次光以外の反射率が常に0なのは正しいか」「S4と一致するか」。
- **列1本でピッチ全体を占める場合は物理的に回折格子になっていない**: この場合
  `structure_builder.columns_to_layers()`が出す各層はピッチ全幅を単一材料で埋める
  セグメントになり、横方向の周期パターンが存在しない。つまり光学的には単なる平坦
  多層膜(DBR)であり、非0次への回折結合係数は**角度・波長に関わらず厳密にゼロ**になる
  （エバネッセント次数が減衰する話ではなく、そもそも格子ベクトルが立たないため）。
  複数列を追加して実際に横方向パターンを作った場合は、[R8_koumyou_v4.py:854](R8_koumyou_v4.py#L854)
  のUIキャプション通り回折格子方程式 `|sinθᵢ + mλ/(Λ·n_env)| ≤ 1` に従い、エバネッセント
  な次数のみ0になる（`RCWA2D/rcwa_mh.py:163`の`Re(kzc)/Re(kzc[p])`効率因子、
  `RCWA2D/rcwa_s4_backend.py:285`のS4側`GetPowerFluxByOrder`実部処理、いずれも正しく
  この打ち切りを実装している）。
- **S4との一致を実測で確認**: WSL側の`~/s4env`（2.1節に既存の環境、今回も問題なく動作）
  で`rcwa_mh`とS4を直接比較するスクリプトを実行（上記構造、角度0°/30°/60°×波長
  400〜780nm、norder=11）。非0次はrcwa_mh・S4とも厳密に`0.00e+00`、0次(Rp/Rs)は
  `max|diff|≈9.4e-5`で、独立実装同士のnorder打ち切り差として妥当な範囲で一致。
- **v3.pyについても同じ質問があり、コード比較だけで結論**: `diff`で`R8_koumyou_v3.py`と
  `R8_koumyou_v4.py`の計算ラッパー（`Rcwa1d_cached`/`calc_rcwa1d_mh`/`calc_rcwa1d_s4`/
  `calc_rcwa1d`）を比較したところ**完全に同一（diff結果ゼロ）**。かつ非graded構造では
  両ファイルとも`layers = columns_to_layers(st.session_state.columns)`という同一呼び出しに
  なるため、上記の結論はv3.pyにもそのまま当てはまるとコード上で確認済み（再計算は
  不要と判断——v3/v4は今後もこのRCWA計算コアを共有している前提で良い）。
- **運用メモ（新規）**: Windows側のセッションから`wsl.exe -e bash -lc "source
  ~/s4env/bin/activate && python3 <script>"`の形で、Ubuntu側の対話シェルを開かずに
  S4環境を直接呼び出せることを確認した。スクリプト自体はWindows側のパス（例:
  セッションのスクラッチパッド配下）を`/mnt/c/...`に読み替えて渡せば実行できる。
  2.2節の`bash ~/run_koumyou.sh`（アプリ本体の起動）とは別に、単発の検証スクリプトを
  都度WSL越しに実行する軽量な手段として使える。
- このセッションはQ&Aのみで、アプリ本体・`RCWA2D/`配下のコードには一切変更なし
  （比較用の一時スクリプトはスクラッチパッドに置いただけで未保存・未コミット）。

**【2026-09-24】MEEP3D「基板+円盤」ケースのQ_scatter,up負値問題を解決 —
2つの独立なバグを発見・修正**: 2026-09-17時点で未解決だった「基板ありの円盤
ケースでQ_scatter,upが一貫して小さい負値になる」件（本節上記参照）の続き。
上記の優先タスク1・2を実行する過程で、当初の想定（box_marginが狭すぎる
だけ）より根が深い、独立した2つのバグが見つかった。

1. **flux boxの底面(z_bottom)貫通バグ（`MEEP3D/geometry.py`)**: 修正前は
   `z_bottom = box_margin`が基板表面(z=0)からの固定オフセットで、円盤の
   高さ(height_um)を全く考慮していなかった。height_um > box_margin
   （現実的な膜厚では普通に起こる）だと底面モニタ面が円盤内部を貫通し、
   3-run減算法（vacuum/bare_substrate/disk）の前提（モニタ面上の背景材質が
   run間で一致）が崩れる。デフォルト値(height=100nm=box_margin=0.1um)は
   ちょうど境界に一致するため症状が軽く出ていた。height=300nmで意図的に
   検証すると、底面のflux値が他の面の10〜100倍かつ符号反転し、
   Q_scatter,upが-4〜-6という値になることを実測で確認した。
   **修正**: 球のMie検証(diag_meep_sphere_mie.py)と同じ「対象物を全方向に
   box_margin分の余白で完全に包む」閉じた箱に変更(`z_bottom = -box_margin`、
   円盤の下端から基板内部に沈める)。円盤がどんな高さでも常に閉じた箱として
   成立するようになった。ただし底面が基板内部にあるため、Q_scatter,upの
   定義が「上方向のみ(基板透過分を除く)」から「円盤という摂動が全方向
   (上・側方・基板透過の変化分)に再配分した総電力」に近い量へ変わった
   点に注意(詳細・トレードオフの検討はgeometry.pyのdocstring参照。ユーザー
   確認済み: 常に閉じた箱にする方式Aを選択)。
2. **入射光量(i0)の符号バグ（`MEEP3D/simulate.py`)**: 円盤パイプラインの
   光源は-z(下向き)伝搬だが、i0の基準に使っていた'top'面はz法線で、MEEPの
   flux符号規約は+zが正——伝搬方向と規約の正方向が逆なため、
   `fluxA['top']`は常に負の値になっていた(実測で全周波数で負であることを
   確認)。sphere-in-vacuum検証では伝搬+xで、i0に使う'x-'面が規約の正方向と
   偶然一致していたため、この非対称なアナロジーの見落としに気づかれずに
   残っていた。この符号違いにより、**Q_scatter,upの符号が常に反転**していた
   （1のバグを修正しても解決しなかった原因はこれ——基板を真空(n=1)にして
   「円盤だけが真空に浮いている」状態でも同じ負の値が出たことで、原因が
   基板側ではないと切り分けられた）。
   **修正**: `q = _scattering_efficiency_from_fluxes(fluxC, ..., -fluxA['top'])`
   と符号を反転。

両方を修正した結果、無損失材質なら球のMie散乱効率と同様に**Q_scatter,upが
常に0以上**になるという唯一のサニティチェックが実際に通ることを確認:
- 円盤材質=基板材質（ゼロテスト）、通常コントラスト(TiO2/SiO2)、円盤高さが
  box_marginを明確に超えるケースの3パターンで全て0以上（`diag_meep_disk_boxmargin.py`
  に再現手順・詳細な面別flux値を残した——今後の回帰テストとして使えるよう
  一時スクリプトから恒久的な検証スクリプトに整理済み）。
- 実際にアプリが呼ぶ本番コードパス`run_scattering_spectrum()`
  （resolution=30, 11波長点, 400-800nm, TiO2円盤/SiO2基板）でも全点0以上
  （1.12〜3.79、波長に対してなめらかに変化）を確認。
- **新たな未解決点（次回セッションへ）**: 符号修正後に解像度収束を確認した
  ところ（resolution 30→60, 3波長点）、650nmでは差2.4%、531.8nmでは4.3%だが
  **450nmでは差15%**とまだ収束していない（`diag_res_convergence.py`は
  スクラッチパッドに置いただけで未保存——再現する場合は同様のスクリプトを
  書き直すこと）。HANDOVER元計画のステップ3（解像度収束30→60→100）を
  継続する必要がある。100px/um以上で収束を確認するまで、Phase 1aの定量的な
  数値（絶対値としてのQ_scatter,up）は参考値扱いとすること。
- `meep3d_app.py`（Streamlit UI）はこのセッションでもまだブラウザで起動
  確認していない（次回の「未実施」項目として持ち越し）。

変更ファイル: `MEEP3D/geometry.py`, `MEEP3D/simulate.py`（コメント更新含む）,
`meep3d_app.py`（UI説明文をQ_scatter,upの新しい定義に合わせて更新）,
`diag_meep_disk_boxmargin.py`（新規、恒久的な回帰テストスクリプト）。

以下は上記の突破口が見つかる前の記述（経緯として残す）:
現在、**S4・rcwa_mh・COMSOLは互いにほぼ一致する場合と、大きく食い違う場合がある**。
食い違いの原因はまだ完全には特定できていない。直近でユーザーから新しい実測構造
（4200nm周期・8列・段差235nm）とCOMSOLの解析データが提供され、この構造での
RCWA・S4・FDTDの三者比較と原因調査が次の課題になっている。

## 2. 環境構成

### 2.1 なぜWSLが必要か
S4はLinux/Mac向けのツールで、Windowsネイティブでのビルドは非常に困難。そのため
**WSL2 (Ubuntu 26.04)** 上に専用のPython仮想環境 `~/s4env` を構築し、そこに
`rcwa_mh`（Linux向けに再ビルド）と`S4`（phoebe-p/S4フォーク）の両方をインストール
してある。Streamlitアプリ自体もWSL側の`~/s4env`で実行する。

ネイティブWindows側（`koumyou_sim/.venv`）でも`rcwa_mh`（Windows用`.pyd`）は動くが、
**S4は使えない**（WSL限定）。

### 2.2 起動方法
Ubuntuターミナルで:
```bash
bash ~/run_koumyou.sh
```
中身は:
```bash
source ~/s4env/bin/activate
cd "/mnt/c/Users/bozu1/OneDrive/デスクトップ/lab/koumyou_sim"
streamlit run R8_koumyou_v3.py
```
起動後に表示される`Local URL`（例: http://localhost:8501）をWindows側のブラウザで
開く（WSL2は自動でポート転送する）。

**注意点（ハマりどころ）**:
- `streamlit run` を使うこと。プレーンな`python R8_koumyou_v3.py`では動かない
  （NoSessionContextエラーになる）。
- 実行時のカレントディレクトリは`koumyou_sim/`であること（`data/nk`への相対パス
  に依存しているため）。
- WSL上で`pkill -f "streamlit run ..."`のようなパターンを使うと、**自分自身の
  bash -lcコマンドライン文字列にも"streamlit run"という文字列が含まれるため
  自己マッチして即死する**。別々のコマンド呼び出しに分けること
  （`wsl -- pkill -f "streamlit run"`単体で呼ぶなど）。
- PowerShell/Bashツールから`wsl`コマンドの出力がUTF-16起因で文字化けすることがある
  （読めばだいたい意味は分かる）。Git Bashから`/mnt/c/...`のようなパスを`wsl`に渡す
  ときはパス変換で壊れることがあるので `MSYS2_ARG_CONV_EXCL="*"` を先頭に付ける。
- 初回`streamlit run`はメール登録のオンボーディング画面が出る。
  `~/.streamlit/config.toml` に `[browser]\ngatherUsageStats = false` を書いて
  おけば次回から出ない。

## 3. ディレクトリ構成

```
koumyou_sim/
├── R8_koumyou_v4.py          # 2026-08-27〜: なめらか基板上げ(graded raise)対応版
│                             #   （run_koumyou.shはまだv3を指しているので手動起動）
├── R8_koumyou_v3.py          # メインのStreamlitアプリ（旧R9_koumyou_v2.py）
├── R8_koumyou_v2.py          # さらに旧いバージョン（現状は使っていない）
├── requirements.txt          # 2026-08-27追加: Windows側.venv用の依存パッケージ一覧
│                             #   （Python 3.11系が必須。1章末尾のマシン移行手順参照）
├── verification_archive/     # 2026-08-26archive: 解決済みのRCWA/S4/Gsolver/COMSOL比較
│                             #   検証一式（comsol_reference*.py, diag_*.py,
│                             #   gsolver_reference_*.py と関連data/を集約。詳細後述）
├── run_koumyou.sh            # WSL起動スクリプト（本体）
├── run_koumyou_fdtd.sh       # FDTD版の起動スクリプト（未検証、要確認）
├── fdtd_app.py, fdtd_demo.py # FDTD関連のアプリ/デモ（未検証、要確認）
├── data/
│   ├── nk/                   # 材料光学定数（.nk形式: 波長 n k）
│   ├── comsol_st235_new.csv  # 【新構造】4200nm/8列/st235ケースのCOMSOL実測値（新規保存）
│   └── gsolver_flat_stack_15layer.csv  # 平坦15層スタックのGsolver計算値（2026-08-20保存）
├── RCWA2D/                   # RCWAソルバー本体＋共通ジオメトリ変換（パッケージ化済み）
│   ├── rcwa_mh.py             # Python参照実装
│   ├── rcwa_mh_cpp.cpp        # C++実装のソース
│   ├── rcwa_mh.cp311-win_amd64.pyd        # Windows向けビルド済みバイナリ
│   ├── rcwa_mh.cpython-314-x86_64-linux-gnu.so  # WSL/Linux向けビルド済みバイナリ
│   ├── rcwa_s4_backend.py     # S4バックエンド（日本語docstring化済み）
│   ├── structure_builder.py   # 列構造→RCWA水平層リストへの変換（両バックエンド共通）
│   │                          #   2026-08-27: expand_graded_columns()追加
│   │                          #   （なめらか基板上げプロファイルの前処理、詳細は4節末尾）
│   └── eigen-3.4.0/           # C++ビルド依存（Eigenヘッダオンリー）
└── fdtd2d/                    # 2D FDTDエンジン（未検証、次の課題）
    ├── engine.py, cpml.py, materials.py, geometry.py, simulate.py, color.py
```

インポートは `from RCWA2D import rcwa_mh` / `from RCWA2D.structure_builder import ...` /
`from RCWA2D.rcwa_s4_backend import ...` の形（パッケージとして`__init__.py`あり）。

## 4. これまでの調査の要点（時系列）

1. **最初の疑い**: 「列（column）を複数に分解する周期構造」でRCWAの計算がおかしいの
   ではないか、という疑惑からスタート。
2. **S4バックエンドを新規構築**（`RCWA2D/rcwa_s4_backend.py`）し、`rcwa_mh`との
   セカンドオピニオンとして使えるようにした。API仕様はS4ソース
   （`S4/main_python.c`）を直接読んで確認済み（phoebe-p/S4フォークを使用。本家
   victorliu/S4はPython3バインディングが壊れていてビルド不可）。
3. **「側面コーティング(sidewall coating)」モデルを試して削除**: 当初、段差の
   垂直壁に材料が回り込まないことが原因かと仮説を立てて実装したが、後に
   COMSOLの実際の断面図（垂直・シャープなエッジ）から「側面コーティングなし」が
   正しいと確認できたため、このモデルは**削除済み**（git履歴に残る）。
4. **上げパターン・基板屈折率の指定間違いを発見**: 当初使っていた「列2,4,6が
   上げ」「基板n=2.785+3.98j」は誤りで、正しくは「列1,3,5が上げ」「基板
   n=1.71+2.88j」（COMSOLファイル名`A1_B0_C1_C0_B1_C0`とユーザーからの直接確認で
   判明）。修正後、桁レベルでCOMSOLと一致するようになった。
5. **残った不一致（470/540nm付近でCOMSOLの方が反射率が高い）の原因調査**:
   - nOrder収束は確認済み（43次で201次までほぼ変化なし） → 原因ではない
   - `rcwa_mh` と `S4` は独立実装同士で0.01以下の誤差で一致 → 「複数列分解」の
     バグではない
   - 列構造を外した平坦15層スタックだけならR≈0.99とCOMSOLのピーク高さに近い
     値が出る → 多層膜レシピ自体は正しい
   - 上げ高さに対する感度が非常に高い（0nm:0.99 → 100nm:0.20、なめらかな
     単調減少） → わずかなパラメータ誤差でも大きな差が出ることが分かった
   - **この時点で明確な結論には至っていない**。COMSOLの断面図が上下ともに
     切れた画像しか得られず、正確な形状の再確認ができなかった。

   これらの検証スクリプトは `diag_convergence.py`, `diag_mh_vs_s4.py`,
   `diag_flat_stack_sanity.py`, `diag_raise_sensitivity.py`,
   `diag_comsol_comparison.py`, `diag_comsol_fullspectrum.py` として残っている
   （すべて`comsol_reference.py`の旧構造を参照）。

6. **Gsolverによる平坦15層スタックの追加検証（2026-08-20）**: ユーザーから
   `diag_flat_stack_sanity.py`と同じ平坦15層スタック（列パターンなし、380〜780nm
   を10nm刻み）のGsolver計算結果が提供された。`data/gsolver_flat_stack_15layer.csv`
   に保存し、`diag_flat_stack_vs_gsolver.py`で`rcwa_mh`と比較。
   - 主stopband（470〜560nm、R≈0.97〜0.99）は平均|diff|≈0.02、最大0.06で良く一致。
     `rcwa_mh`・`S4`に続き**3つ目の独立ツール**でも「多層膜レシピ自体は正しく、
     このDBRは深いstopbandを持つ」ことが裏付けられた。
   - stopband外の細かい干渉フリンジ（400, 590, 640〜650, 670〜680, 750〜760nm付近）
     では最大0.12〜0.20の差がある。10nm刻みサンプリングでフリンジ位置がツール間で
     わずかにずれている可能性が高く、実装バグの兆候ではなさそうと判断。
   - **注意**: これは平坦（列パターンなし）構造の検証であり、5.で不一致が残る
     「列パターン＋段差」構造そのものを解決するものではない。あくまで「多層膜
     レシピは正しい」という既存の結論の補強材料。

7. **st235構造でのrcwa_mh/S4/FDTD計算とhypothesisテスト（2026-08-19〜20、5.4の1〜3が
   相当進行済み）**: `comsol_reference_st235.py`作成済み。`diag_st235_rcwa_mh.py`
   （rcwa_mh、41波長×9角度×2偏光フルグリッド、`data/rcwa_mh_st235_result.json`に保存）、
   `diag_st235_s4.py`（S4、代表サブセット）、`diag_st235_fdtd.py`（2D FDTD、
   `data/fdtd_st235_result.json`。ただしTE/s偏光・法線入射のみ対応というfdtd2dの制約
   あり）を実行済み。COMSOLとの不一致は解消されていない。
   `diag_st235_hypotheses.py`/`diag_st235_hypotheses2.py`で「上げ列パターンを反転」
   「基板屈折率の虚部符号を反転」の2仮説をテストしたが、いずれもbaselineより悪化
   （改善なし）。
8. **「平坦界面」堆積モデルはst235では不一致を悪化させると判明（2026-08-20）**:
   アプリのUIヘルプ文言（旧R8_koumyou_v3.py）が「平坦界面＝COMSOL/Gsolverのデフォルト」
   と明言していたのに、st235の比較スクリプトは一度も試していなかったため検証した。
   結果、波長サブセット(6点)×全9角度×両偏光で mean|diff|: 等形成膜0.13 → 平坦界面0.33、
   RMS: 0.17 → 0.42と**悪化**（両モデルとも決定的な一致には至らない）。旧構造での
   `diag_conformal.py`の検証と合わせ、「等形成膜 vs 平坦界面」は不一致の原因ではないと
   判断。この結果を受けてユーザーの指示により、R8_koumyou_v3.py の「積層モデル
   (Deposition model)」切替UI（平坦界面モード・グローバル膜層エディタ・
   flat-alignチェックボックス一式）と`structure_builder.py`の`columns_to_layers_flat()`
   を**削除済み**。アプリは常に等形成膜（列ごとに基板面から膜を積む）のみになった。
   （検証に使った一時スクリプトは削除済み。再検証する場合は
   `columns_to_layers(columns, flat_align=True)` で列の薄膜開始高さだけ揃える簡易版が
   `structure_builder.py`に残っているので、必要ならそこから作り直すこと。）

## 5. 新しい課題（st235は5.4の1〜3が進行中 — 詳細は4.の7,8番も参照）

ユーザーから、**全く別の新しい構造**とCOMSOL実測データが提供された（この
セッションの最後）。旧構造（3000nm/6列）とは別物なので注意。

### 5.1 構造仕様（確定情報）
- TiO2/SiO2周期多層膜、**7.5ペア**（最上層・最下層ともにTiO2）
  - 膜厚: TiO2 = 77.6nm, SiO2 = 54.4nm
  - 屈折率: TiO2 n=2.3, SiO2 n=1.45（定数、吸収なし）
- **基板屈折率: n = 1.71 + 2.88i**（ユーザーが一度「-2.88i」と書いたが、直後に
  訂正して「+2.88i」が正しいと確認済み。旧構造の値と同じ符号）
- **基板上げ高さ: 235nm**（画像ラベルより「λ0/2 = 235nm」の設計）
- **周期（ピッチ）: 4200nm**
- **列構成（8列、左から右）**: 幅 `[900, 300, 900, 300, 600, 300, 600, 300]` nm
  - 上げ列（235nm）: **幅300nmの列すべて**（4列とも上げ）。900nm・600nm幅の列は
    上げなし。
  - 上げ割合 = 1200/4200 = 28.5%（画像の「基盤上げの割合: 28.5%」と一致）
  - COMSOLファイル名 `A0_C1_A0_C1_B0_C1_B0_C1_st235_new` から: A=900nm, B=600nm,
    C=300nm、フラグ`0`=上げなし・`1`=上げあり、と解読すると上記と完全に一致
    （3つの独立情報源＝画像の寸法・画像の上げ割合・ファイル名、すべてで検証済み）。
  - **ユーザーの強調点**: 「この構造はあえて段を食い違わせている」
    （非対称・非周期的な配置になっている点を見落とさないこと）。

### 5.2 COMSOL実測データ
`data/comsol_st235_new.csv` に保存済み（このセッションで新規作成）。
- 列: `eta_P, lamda(nm), theta(deg), freq(THz), Reflectance order 0`
- `eta_P=0` → s偏光、`eta_P=1` → p偏光（旧データセットと同じ命名規則と推定。
  要再確認）
- 波長: 380〜780nmを10nm刻み（41点）
- 角度: 0, 10, 20, 30, 40, 50, 60, 70, 80度（9点）
- 合計 41×9×2 = 738行

**注意**: このCSVをこのセッションで手打ち転記した際に、1箇所
（`1,759.9999999999999,0,...`の行）に値が誤って重複挿入されるミスがあり、
その場で修正済み。念のため他の行も異常値がないか目視確認しておくと安心。

### 5.3 ユーザーの仮説
「境界条件の設定に問題があるのではないか」との指摘あり。文字通りの意味での
境界条件（周期境界・PML等）の可能性もあるが、**セッション中に基板屈折率の
符号を一瞬「-2.88i」と書き間違えた実例があった**ことから、複素屈折率の
時間依存性の符号規約（`exp(-iωt)`系なら損失媒質はn+ik、`exp(+iωt)`系ならn-ik）
の食い違いという可能性も要検討。COMSOLの内部規約とrcwa_mh/S4の規約が違う場合、
**同じ物理的な材料でも符号を逆にして入力しないと一致しない**。これまでの
検証では一貫して「+2.88j」を使ってきており、それでエネルギー保存則や
物理的に妥当な挙動が確認できているため、rcwa_mh/S4側の規約は「損失媒質は
+ik」で間違いないはず。COMSOL側の規約がこれと同じか異なるかは未確認。

### 5.4 やるべきこと
1. `comsol_reference.py`を参考に、新構造用の参照モジュールを作成する
   （例: `comsol_reference_st235.py`）。8列・4200nmピッチ・上記の上げパターンを
   `structure_builder.columns_to_layers()`で組み立てる。
2. `rcwa_mh`と`S4`の両方でこの新構造を計算し、`data/comsol_st235_new.csv`の
   実測値と比較する（`diag_comsol_comparison.py`と同様の枠組みが流用できる）。
   計算量が大きい（9角度×41波長×2偏光）ので、まずは代表的な部分集合
   （例: AOI=0/30/60、全波長）で素早く傾向を掴んでから範囲を広げるのが良い。
3. `fdtd2d/`のFDTDエンジンを調査し、同じ構造で計算できるようにする。
   このセッションでは**まだ中身を全く検証していない**（`engine.py`,
   `cpml.py`, `materials.py`, `geometry.py`, `simulate.py`, `color.py`が存在する
   ことを確認しただけ）。`fdtd_app.py`, `fdtd_demo.py`, `run_koumyou_fdtd.sh`も
   併存しているが動作未確認。まずこれらを読んで、分散性材料（Lorentzモデル等）・
   斜入射・周期境界条件がどう実装されているか、`rcwa_mh`/`S4`と同じ符号規約
   （n+ikで損失、等）を使っているか確認すること。
4. RCWA・S4・FDTD・COMSOLの4者を比較し、一致しない場合は上記5.3の仮説
   （符号規約／境界条件）を軸に切り分けること。
5. 旧構造（3000nm/6列, 470/540nm不一致）の調査も並行して未解決のまま残っている。
   新構造での知見が旧構造の謎解きにも使える可能性がある。
6. **【2026-08-20開始・進行中】st235/旧構造とは別の「段差＋列」ジオメトリでの追加検証**:
   ユーザーが「他の周期多層膜でも比較したい」と提案。COMSOLは都度モデル作成が必要で
   重いため、**Gsolverのテキストインポート機能（6.参照）で列構造を直接表現し、
   ローカルで即座に4つ目の独立ツールとして参照値を得る**方針で合意。参照データは
   まだ準備できていない（構造候補もまだ確定していない）。
   - GSolver.iniの材質カタログを確認したところ、`TiO2: 2.3, 0` / `SiO2: 1.45, 0` /
     `SUS: 1.71, 2.88` と、こちらの想定値と完全に一致していた（6.の懸念点は解消済み
     — 詳細は6.の該当項目を参照）。すぐにテキストインポートを試せる状態。
   - `structure_builder.py`の`columns_to_layers()`が出す`layers`
     （top-first、各層`{'thickness', 'segments': [(material, width_fraction), ...]}`）
     を、substrate-first・`[LAYER]`ブロック形式のGsolverインポート用テキストに変換する
     コンバータがまだ存在しない。これが未着手の次のステップ。
   - **未検証・要注意**: Gsolverのテキストインポート機能自体、このプロジェクトで
     一度も実際に使ったことがない（`data/gsolver_flat_stack_15layer.csv`はユーザーが
     GsolverのUIで手動構築した平坦スタックの結果であり、テキストインポート経由では
     ない）。特に`<true/false>`フラグの意味が不明（マニュアル未確認）。初回は
     GsolverのUI上でインポート後の断面プレビューを目視確認してから計算を回すこと。
   - 上記コンバータ経由の比較はまだ結果待ち（`data/gsolver_import_2col.txt`,
     `data/gsolver_import_8col_altraise.txt`をユーザーがGsolverでインポート→RUNする
     のを待っている段階）。
   - **【2026-08-24 重要な結果】ユーザーが別途、Gsolver上で直接（テキストインポート
     を使わず手動構築で）新しい6列構造を計算し、`data/gsolver_6col_v2.csv`として
     結果を提供**: 周期3000nm・6列・幅[900,300,600,300,600,300]nm、幅900/600/600の
     列（インデックス0,2,4）のみ235nm上げ・幅300nmの列は上げなし。多層膜レシピ・
     基板n=1.71+2.88jはst235と同一（ユーザー確認済み）。Gsolver側はs偏光(TE)で計算
     （ユーザー確認済み）。`gsolver_reference_6col_v2.py` + `diag_6col_v2_vs_gsolver.py`
     で`rcwa_mh`のs偏光結果と比較した結果:
     **mean|diff|≈0.22, RMS≈0.26, max|diff|≈0.58 と、st235/旧構造でのCOMSOLとの
     不一致と同程度かそれ以上に大きく食い違う。** これはCOMSOLを一切介さない
     rcwa_mh–Gsolver間の直接比較であり、**「COMSOLの規約・設定の問題」という
     仮説に対する強い反証材料**になる。列＋段差構造でのrcwa_mhの不一致は
     ツール固有の設定差ではなく、より根本的な原因（列分解ロジック自体、
     あるいは複数の独立ツールに共通する別の物理的前提の違い）を疑うべき段階に
     来ていると考えられる。
     - **副次的な発見**: 検証コード内で「法線入射ならs/p偏光は縮退するはず」という
       誤った前提でs/pの一致チェックを入れていたところ、max|Rs-Rp|≈0.30という
       大きな差が出た。考え直すと、**平坦な多層膜スタックは法線入射でs/p縮退するが、
       列（グレーティング）構造は周期方向に面内回転対称性が破れているため、
       法線入射でもs/pは一致しない**（物理的に正しい）。今後、他ツールとの
       比較の際は必ず偏光設定を明示的に確認・一致させること
       （st235のCOMSOLデータではeta_P列で区別済みだが、思い込みで判断しないこと）。
     - 次にやるべきこと: (a) このrcwa_mh–Gsolverの不一致がS4でも再現するか確認
       （独立ソルバーでの追試）→ **実施済み、下記参照**。(b) 収束次数(NORDER=43)を
       上げても変わらないか確認 → **実施済み、下記参照**。(c) 上記の2col/8col_altraise
       候補のGsolver結果が揃い次第、同様の直接比較を行い、この6列構造だけの特殊事情
       でないか確認。

   - **【2026-08-24 追加】原因究明のための仮説スイープ（`diag_6col_v2_hypotheses.py`,
     `diag_6col_v2_hypotheses2.py`）**: 上記(b)を含む5系統の仮説をこの6列構造で
     機械的にテストした。
     - **NORDER収束（43/61/81）**: mean|diff| 0.2194→0.2213→0.2216とほぼ変化なし。
       43で既に収束しており、次数不足が原因ではない。
     - **flat_align=True**: mean|diff|は0.197とやや改善するが、RMS(0.286)とmax(0.885)
       はむしろ悪化。一貫した改善ではない。
     - **基板屈折率の符号反転（n=1.71-2.88j）**: mean|diff|=5.35, max|diff|=71.5と
       物理的に破綻するレベルの数値になった。+2.88jが正しい規約であることを極めて
       強く再確認。
     - **上げ列パターンの反転（幅900/600/600ではなく幅300nmの列3本を上げる、
       st235と同じ側）**: mean|diff|=0.163, RMS=0.198, max|diff|=0.415と、
       **3指標すべてで一貫して改善**（baseline比で約25〜30%減）。これまで試した
       仮説の中で唯一、方向性の一貫した改善が見られた。
     - この反転パターンでの上げ高さスイープ（100〜300nm）では h=220〜235nm付近が
       最良（mean|diff|≈0.16）で、ユーザー申告の235nmとほぼ一致 — 上げ高さの値自体は
       妥当と考えられる。反転パターン+flat_align=Trueの組み合わせはさらに悪化
       （mean|diff|=0.263）し、flat_alignは常に悪化要因。
     - **結論**: 上げ列反転が最も筋の良い手がかりだが、最良ケースでもmean|diff|≈0.16
       とまだ大きな不一致が残る。完全解決には至っていない。

   - **【2026-08-24 決定的な結果】S4によるクロスチェック（`diag_6col_v2_s4.py`,
     WSL実行）**: 両方の上げパターン（baseline: 900/600/600上げ、inverted:
     300nm×3上げ）で、**S4とrcwa_mhは機械精度で完全一致**（S4 vs rcwa_mh:
     mean|diff|=0.0000, max|diff|=0.0001、両パターンとも）。したがって
     S4もrcwa_mhと全く同じだけGsolverと食い違う（baseline: mean|diff|=0.2194、
     inverted: mean|diff|=0.1626 — rcwa_mh単体の結果と完全に同一）。
     - **これは極めて重要な切り分け結果**: 独立に実装された2つのRCWAソルバー
       （自作`rcwa_mh`とStanfordの`S4`）が、同じ`structure_builder.py`の列分解
       ロジックを通した同一の`layers`に対して寸分違わず一致する以上、
       **「RCWAソルバー実装のバグ」という可能性はほぼ排除できる**。残る容疑は
       (a) `structure_builder.py`が共有している幾何モデルの前提（列ごとに
       基板面から膜を積む「等形成膜」モデル）自体が、Gsolverでの実際の計算内容と
       一致していない、または (b) Gsolver側の設定（列幅・上げ高さ・材質・境界条件
       など）に、こちらが把握できていない相違点がある、のいずれか。
     - **次にやるべきこと**: 上げ列反転以外の仮説（flat_align, 上げ高さ,
       基板符号）は一通り潰した上での結果なので、次は
       (a) Gsolver側の設定をユーザーと一緒にもう一度突き合わせる（列の並び順、
       周期境界条件の設定、材質カタログの参照先など、数値では見えない設定項目）。
       (b) `structure_builder.py`の等形成膜モデルの前提そのものを疑い、
       Gsolverが実際にどう列境界をメッシュ化・計算しているかを確認する
       （UserManual参照、または単純な2材質1層グレーティングでの基礎検証）。
       → **(b)実施済み、下記参照**。

   - **【2026-08-24 UserManual61.pdf精読による新知見】**: ユーザーがGSolver公式
     マニュアル全文を提供、内容を精読した。
     - **`<true/false>`フラグの意味が判明（section 2.3.1）**: 「波長が変わった時に
       屈折率を再計算するかどうか」のフラグであり、幾何形状とは無関係。TiO2/SiO2/SUS
       はすべてCONSTANT型（波長非依存）なのでこの値は無意味 — `RCWA2D/gsolver_export.py`
       が常にfalseを出力する実装で問題ないことが確定した。
     - **GSolverの内部表現は常に「水平層×各層内の横方向ブロック（材質・幅比）」の
       一種類のみ（section 1.1, 4.4）**: Canvas描画・GS4 Editor・テキストインポートの
       どの方法で構築しても、最終的にはこの同一形式に帰着する。これは
       `structure_builder.py`の`layers`形式そのものと同じ。つまりGSolver側に
       「等形成膜 vs 平坦界面」を切り替えるスイッチのようなものは存在せず、
       渡す幾何形状データ次第でしかない。テキストインポートファイルは列ごとに
       段差に沿って膜が始まる構造を正しくエンコードできている（目視確認済み）ため、
       この観点でのモデル不一致は考えにくい。
     - **【新しい有力仮説】Canvas Grid量子化誤差（section 4.2, 1.3.4）**:
       GSolverのGraphical Editor（Canvasに矩形を描画する方式）で作った構造は、
       **Canvas Gridの解像度に寸法が量子化（丸め込み）される**。「corner featureは
       材料内でλ/10未満にすべき」という経験則も記載あり。周期3000〜4200nmに対し
       幅300nmの列（周期の7〜10%程度）はこの精度の影響を受けやすい規模。
       **ユーザーに確認したところ、G比較2.csv（6列構造）は実際にGraphical Editorで
       矩形を手描き（Canvas方式）して作ったとのこと**——量子化誤差が疑わしい。
       一方、平坦15層スタック（良く一致していた方）は列パターンがなく全幅1つの
       矩形で済むため、この量子化の影響を受けない。「列パターンがある構造だけ
       大きくずれる」という観測パターンと整合する、これまでで最も筋の良い説明。
     - また、GSolver側の`ORDERS`（Fourierトランケーション次数）はユーザーが別途
       設定する値で、こちらのNORDER(=43/61/81)とは独立。今回の6列構造の実行で
       いくつを使ったかは未確認。
     - **対応**: `RCWA2D.gsolver_export.write_gsolver_import()`を使い、
       6列構造（`gsolver_reference_6col_v2.py`）のテキストインポート版を生成
       （`data/gsolver_import_6col_v2.txt`）。これはnm単位の寸法をCanvas Grid量子化
       なしに厳密な幅比（分数）としてエンコードするため、この仮説を切り分けられる。
       マニュアルには「テキストインポート後はApproximateボタンを押さないこと
       （押すとCanvas Grid解像度に再量子化されてしまう）。直接Listing/RUNか
       GS4 Editorに進むこと」との明記あり（section 2.3.1）——重要な運用上の注意点。
     - **【2026-08-24 解決】最終結論**: ユーザーがGSolverで実際に検証してくれた。
       (1) ORDERSデフォルト(3〜5) + Canvas手描き → mean|diff|≈0.22（最初の不一致）。
       (2) ORDERSデフォルト + テキストインポート（正確な寸法） → mean|diff|≈0.25
           （量子化誤差だけでは説明できない、むしろ悪化 — 次数不足の誤差の方が支配的
           だったため）。
       (3) **ORDERS=43 + テキストインポート → mean|diff|=0.0071**（`rcwa_mh`とほぼ
           完全一致、41点中ほぼ全点で誤差0.01未満）。
       (4) **ORDERS=43 + Canvas手描き → 波長に対して滑らかでない、ジグザグな
           非物理的スペクトル**（rcwa_mhと一致しない上に、そもそも収束した解に
           見えない）。
       **結論: GSolverでこの種の列＋段差構造を正しく計算するには
       (a) ORDERSを十分高く（43以上）、かつ (b) 寸法をテキストインポートで
       正確に指定する — の両方が必要**。確実に言えるのはテキストインポート＋
       ORDERS=43の組み合わせが`rcwa_mh`と完璧に一致したという一点。

       **【2026-08-24 訂正】** 当初「低ORDERSでは量子化誤差が次数不足の誤差に
       埋もれて見えず、高ORDERSにして初めてCanvas量子化由来の不安定さが表面化した」
       と説明したが、これは言い過ぎだった——low ORDERS(=5)のCanvas版・
       テキストインポート版を見直すと、**どちらも多少のギザつきがあり**、
       「高ORDERSだけで急に不安定になる」というほど綺麗な話ではない。
       また、ユーザーから**Canvas手描き版の膜厚パラメータがTiO2=77nm/SiO2=54nm
       （丸めた値）で入力されていたと判明**——rcwa_mh・S4・テキストインポート
       ファイルはすべて厳密値77.6nm/54.4nmを使用しているため、Canvas版は
       そもそも「列幅の量子化」だけでなく「膜厚パラメータ自体の丸め」という
       別の誤差も持っていた（7.5ペアで累積すると約7.5nm相当の厚さ差）。
       **今後GSolverと比較する際は、Canvas手描きは使わず、必ずテキストインポート
       （`RCWA2D/gsolver_export.py`、TiO2=77.6nm/SiO2=54.4nmを厳密指定）
       ＋ORDERS=41以上を使うこと。** Canvas版由来の数値は幾何・膜厚の両方が
       不正確なため、今後の精密な比較には使わない。

   - **【2026-08-26 追加検証・数値安定化とバリエーションテスト】**
     - NaN・不安定な挙動対策として、TiO2/SiO2に数値安定化用の微小損失
       (n=2.30+1e-4j, n=1.45+1e-4j) を追加。`comsol_reference.py`,
       `comsol_reference_st235.py`, `gsolver_reference_6col_v2.py`,
       `gsolver_reference_2col.py`と、GSolver.ini（`TiO2: 2.3, 0.0001` /
       `SiO2: 1.45, 0.0001`）の両方に反映済み。物理的な反射率への影響は
       mean|diff|<0.001と無視できるレベルで確認済み。
       **注意**: GSolverは終了時に現在のメモリ上のカタログでiniファイルを
       上書きするため、この変更を反映するにはGSolverを再起動する必要がある。
     - 旧構造（`comsol_reference.py`）の列幅[900,600,300,300,600,300]nmは
       そのままに、上げ高さ100nm→**235nm**、膜厚を**TiO2=54nm/SiO2=77nm**
       （厳密値77.6/54.4ではなく丸めた値、かつ通常のTiO2/SiO2の役割を入替）
       に変えたバリエーション（`gsolver_reference_oldstruct_235_rounded.py`,
       `data/gsolver_import_oldstruct_235_rounded.txt`）でも同様に検証。
       S4も追加（`diag_oldstruct_235_rounded_s4.py`）し、rcwa_mh–S4間は
       今回もs偏光でmean|diff|=1.4e-5とほぼ完全一致（p偏光はmean|diff|=0.007
       とやや大きいが依然小さい）。
       - 初回のGsolver実行（テキストインポート、ORDERS=43のつもり）は
         rcwa_mh/S4のどちらの偏光とも大きく食い違った（mean|diff|≈0.07〜0.09）。
       - ユーザーがORDERS設定を再確認して再実行したところ、
         **rcwa_mh(s偏光)とmean|diff|=0.0005まで一致**。初回の食い違いは
         ORDERSが実際には43として反映されていなかった（設定のつけ忘れ・
         別ファイルの読み込み等）ことが原因だった可能性が高い。
       - **結論**: テキストインポート＋ORDERS=43＋微小損失という組み合わせは、
         列幅・上げ高さ・膜厚を変えたバリエーションでも再現性よく機能する
         ことが確認できた。GSolverとの比較で結果が合わないときは、まず
         ORDERSが本当に意図通り設定・反映されているかを疑うこと。

     - **koumyouアプリ（R8_koumyou_v3.py）自体でも同じ構造を組んで検証**。
       アプリのUIで列幅[900,600,300,300,600,300]nm・上げ235nm@列1,3,5・
       TiO2=54nm/SiO2=77nmを手動で組んだところ、当初はrcwa_mh参照値と
       mean|diff|≈0.065〜0.066とズレた。原因を切り分けたところ2つあった:
       1. **nOrderのデフォルトが11**（[R8_koumyou_v3.py:41](R8_koumyou_v3.py#L41)、
          画面上部の「nOrder」欄）— こちらの参照計算のNORDER=43よりずっと低い。
          43に変更しても、まだ周期の設定ミスが残っていて一致しなかった。
       2. **周期（Pitch）の設定間違い** — ユーザーが気づいて修正。
       両方を直した後、**mean|diff|=0.0006, max|diff|=0.0036とほぼ完全に一致**。
       これで**GSolver（テキストインポート＋ORDERS=43）・koumyouアプリ
       （nOrder=43・正しい周期）・rcwa_mh単体参照計算・S4の4者すべてが
       同一構造で一致することを確認**できた。列＋段差DBR構造での長期間の
       不一致調査は、これで実質的に決着したと言える。
       **教訓**: 複数ツール間で結果が合わないときは、(a) 次数/ORDERS設定、
       (b) 周期・列幅・上げ高さなどの幾何パラメータ、(c) 膜厚などのレシピ、
       を順に疑い、可能な限り「厳密な数値をテキスト/コードで指定する」方法
       （GUIでの手描き・手入力を避ける）で構造を再現するのが最も信頼できる。
       - **全角度での再確認（2026-08-26）**: 「0°だけの確認では不十分」との
         指摘を受け、koumyouアプリの角度分解Rs出力（-80〜80deg, 10deg刻み,
         17角度×41波長=697点）と`rcwa_mh`参照値（`diag_oldstruct_235_rounded_angles.py`
         で生成、`data/rcwa_mh_oldstruct_235_rounded_angles_result.json`）を
         全点比較。**全角度でmean|diff|=0.0005, max|diff|=0.0036と、角度による
         偏りなく一致**（各角度ごとのmean|diff|も0.0003〜0.0006の狭い範囲に
         収まっている）。法線入射だけでなく斜入射も含めて、koumyouアプリの
         RCWA計算がrcwa_mh参照値と一致することを確認済み。
     - この結論は、旧構造（`comsol_reference.py`、470/540nm不一致、2026-08-04から
       未解決）にも同じ手法（`diag_oldstruct_gen_and_rcwa.py`で生成した
       `data/gsolver_import_oldstruct.txt`をORDERS=41以上でテキストインポート）
       を適用すれば同様に解消する可能性が高い（ユーザーが「一致した」との言及あり、
       正式な数値はまだ未取得 — 次回セッションで確認・記録すること）。
     - **COMSOLとの不一致（st235・旧構造）への示唆**: COMSOLはFEMなのでORDERSでは
       なくメッシュ密度が対応するパラメータ。段差の角付近でメッシュが粗いと同種の
       誤差が出る可能性が高く、次にCOMSOL側のメッシュ設定を確認する価値がある。

   - **【2026-08-26】koumyouアプリに「スクリーンショット」機能を追加**:
     ユーザー参考ツール`Gsolver'S COLOR.html`（Gsolverの角度分解反射率データを
     貼り付けてCIE色スウォッチ・チャートを表示する既存のスタンドアロンHTMLツール、
     `data/`ではなくプロジェクトルート直下）と見た目・レイアウトを揃える形で、
     `R8_koumyou_v3.py`のAngle sweep(3D)モードを再構成した。
     - **`_sd_to_hex`ヘルパーを早い位置に移動**（Colorimetryセクション内のローカル
       関数だったものを、`_idx_for_order`定義の直後に移動し、Angle sweepブランチの
       どこからでも呼べるようにした）。
     - **0°/30°/60°の色スウォッチを追加**（掃引範囲の設定に関わらず、常にこの
       3角度で`calc_rcwa1d`を個別に呼んで計算。偏光はRp/Rsラジオボタンで切替、
       デフォルトRp）。表示位置はAngle sweepブランチの先頭（旧3Dサーフェスプロット
       があった場所）。
     - **3Dサーフェスプロット（Reflectance vs Wavelength & Angle, go.Surface）を
       削除**（ユーザー指示により不要と判断）。
     - **新規チャート「Reflectance vs Wavelength (per angle)」を追加**
       （既存の「Reflectance vs Angle (per wavelength)」チャートの直後に配置）:
       掃引した各角度についてR(波長)を重ねて表示、代表角度(`angle_ar[0]`)を
       黒太線で強調——`Gsolver'S COLOR.html`の「反射スペクトル（角度ごとの
       波長依存性）」チャートと同じ考え方。
     - これで表示順は「色スウォッチ → 角度依存チャート → 波長依存チャート」
       という`Gsolver'S COLOR.html`と同じ並びになった。
     - `diag_app_smoke.py`に「Angle sweep (3D)モードへの切替」チェックを追加し、
       新コードパス（スウォッチ計算含む）が例外なく通ることを確認済み。

   - **【2026-08-26 未解決】新しい2列構造でGsolverとkoumyouの色が違って見える件**:
     `gsolver_reference_2col_51_81.py`（幅900nm＋幅300nm・周期1200nm・幅300nm列を
     235nm上げ、**TiO2=51nm/SiO2=81nm**という新しいレシピ、他は既存構造と同じ
     基板n=1.71+2.88j・7.5ペア・微小損失込み）を作成
     （`data/gsolver_import_2col_51_81.txt`, `data/rcwa_mh_2col_51_81_result.json`、
     `diag_2col_51_81_gen_and_rcwa.py`）。ユーザーがkoumyouアプリ上でこの構造を
     組んでGsolverと見比べたところ、色が違って見えるとの指摘。切り分けた結果:
     - **koumyouアプリ自体の色計算は正しい**: 独立に計算したrcwa_mhの色
       （s偏光, 0°=#00bdea, 30°=#2766d2, 60°=#4440d0）が、koumyouアプリの表示
       （#00bdec, #2566d2, #4542d2）とほぼ完全に一致。
     - **`colour`ライブラリと`Gsolver'S COLOR.html`のJS実装は同じ色を返す**:
       同一スペクトルに対し、`colour.sd_to_XYZ`のデフォルト計算と、
       HTMLツールと全く同じ手順（線形補間→5nm刻みリーマン和→標準sRGB行列→
       ガンマ補正）を手動再現したものを比較したところ、XYZ・hexとも実質完全一致
       （#00bdeb同士）。**色計算方式の違いという可能性は排除できた**（コード修正
       の必要なし）。
     - **回折光（m≠0）の寄与を確認**: 周期1200nmは可視光波長に対して大きいため
       回折光の寄与はかなり大きい（0次光のみ vs 全次数合算で色がはっきり変わる、
       500nmでのR比較で0.16→0.71のように跳ね上がる角度もある）。ただし
       **koumyouのスウォッチは既に0次光のみ（specular, m=0）を使っており**、
       これはGsolverの"Reflectance order 0"という列名・および他構造での高精度な
       一致実績から見て、Gsolver側も0次光のみをエクスポートしているはずなので、
       この違いが原因である可能性は低いと判断。
       比較用に0次光のみ/全次数合算/回折光成分のみの3色を並べたArtifact
       「Diffraction Color Breakdown」を作成し、視覚的に提示した。
     - **未解決点**: 上記はすべて「koumyou側は正しく計算できている」ことの
       確認であり、Gsolver側の実際の数値（0°/30°/60°の反射率スペクトル）と
       rcwa_mhを直接突き合わせる最終確認はまだ行えていない
       （ユーザーからGsolverの生データがまだ提供されていないため）。
       **次回セッションでの最初の一歩**: `data/gsolver_import_2col_51_81.txt`を
       Gsolverでテキストインポート（ORDERS=43以上、Approximateは押さない）した
       実測値を取得し、`data/rcwa_mh_2col_51_81_result.json`と数値で比較すること。

   - **【2026-08-26 運用メモ】`~/run_koumyou.sh`のCRLF問題**: WSL側の
     `~/run_koumyou.sh`がWindows形式の改行コード(CRLF)になっており、
     `set: -: invalid option` / `source: No such file or directory` /
     `cd: ...\r: No such file or directory`で起動が壊れていた。
     `sed -i 's/\r$//' ~/run_koumyou.sh`で修正済み。**もし将来また
     Windows側のエディタでこのファイルを保存した場合、同じ問題が再発しうる
     ので、起動失敗時はまずこれを疑うこと。**
     このセッションの最後にユーザー指示で`wsl -- pkill -f "streamlit run"`
     （単独呼び出し、bash -lcでラップしない——自己マッチ回避のため）を実行し
     Streamlitサーバーは停止済み。次回は`bash ~/run_koumyou.sh`から再起動が必要。

- `data/nk/SUS.txt`→`SUS.nk`にリネーム済み（元は拡張子違いで読み込めなかった）。
  中身はn=1.71, k=2.88の定数データで、基板屈折率の値と整合している。
- `piko/`フォルダ配下（`app.py`, `ppap.py`等）も`import rcwa_mh`しているが、
  今回のRCWA2D/移動に伴うimport修正は**意図的に行っていない**（別系統のファイルと
  判断したため）。必要なら同様に`from RCWA2D import rcwa_mh`へ直す。
- Gsolver（`C:\Users\bozu1\OneDrive\デスクトップ\Gsolver\GSolverV61.exe`）が
  ローカルにインストール済み。COMSOLとは別の比較対象として使える。
  - スクリプト/コマンドラインAPIは無い（マニュアル確認済み）。
  - **テキストファイルによる構造インポート機能あり**（File→Import Text）。
    フォーマット:
    ```
    [LAYER]
    <厚さ(μm)>
    <幅(周期比)> <材質タイプ> <材質名> <true/false>
    ...
    [LAYER]
    ...
    [END]
    ```
    材質タイプは `CONSTANT, TABLE, SCHOTT, SELLMEIER, DRUDE, HERZBERGER, POLYNOMIAL`
    のいずれか。材質名はGSolver.iniのカタログに登録されている必要あり。
    層は基板側から順（layer[0]が基板直上）。
  - 結果もテキストで書き出せる（Grid→Save Grid、Write Fields to File）。
  - 完全自動化（起動→インポート→RUN→出力保存）には、ドキュメント化された
    APIがないためUI自動化（マウス/キーボード操作のシミュレーション）が必要。
    ユーザーとはどこまで自動化するか未確定のまま中断。
  - **【2026-08-20 解消済み、2026-08-26さらに更新】** `GSolver.ini`のデフォルト
    材質カタログには元々`TiO2: 2.45, 0`という、ユーザーの指定値（n=2.30）と異なる
    定数エントリがあり気になる手がかりとして残していたが、確認したところ現在の
    カタログは`TiO2: 2.3, 0` / `SiO2: 1.45, 0` / `SUS: 1.71, 2.88`と、こちらの
    想定値と完全に一致するよう修正済みだった（誰がいつ直したかは不明。おそらく
    平坦15層スタック検証時にユーザーが手動修正した）。**2026-08-26に数値安定化
    のため`TiO2: 2.3, 0.0001` / `SiO2: 1.45, 0.0001`へさらに更新済み**（4節の
    該当項目参照）。GSolverは終了時にメモリ上のカタログでiniを上書きするため、
    この値を使うにはGSolverの再起動が必要。CONSTANT材質としてそのまま
    使ってよい。
